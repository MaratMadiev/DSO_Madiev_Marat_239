title = "Gitleaks Configuration"

[allowlist]
description = "Allow list for false positives"
files = [
  "package-lock.json",
  "yarn.lock",
  "EVIDENCE/**"
]

regexes = [
  '''test.*password''',
  '''example.*token'''
]
